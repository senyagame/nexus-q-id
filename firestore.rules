rules_version = '2';
service cloud.firestore {
  match /databases/nexusqid/documents { // Тут должно быть имя твоей базы!
    match /artifacts/{appId}/public/data/profiles/{userId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}